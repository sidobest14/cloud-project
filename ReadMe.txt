# Tabbed Menu Web App

A lightweight HTML/CSS/JS web app with a tabbed interface and menu simulation.
Containerized using Docker and served via Apache.

# How to deploy the Docker Image

. Copy "docker pull sidobest14/cloud-project" into your CLI and run it
